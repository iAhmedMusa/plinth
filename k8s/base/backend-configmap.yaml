apiVersion: v1
kind: ConfigMap
metadata:
  name: backend-config
  labels:
    app.kubernetes.io/name: backend
    app.kubernetes.io/component: backend
data:
  # comma-separated origins allowed to call the backend (CORS)
  FRONTEND_ORIGINS: "http://devops.localtest.me"
